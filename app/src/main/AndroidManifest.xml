<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/ic_menu_myplaces"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_menu_myplaces"
        android:supportsRtl="true"
        android:theme="@style/Theme.Supermarket"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.admin.ManageProductsActivity" />
        <activity android:name=".ui.admin.ManageOrdersActivity" />
        <activity android:name=".ui.customer.CustomerMainActivity" />
        <activity android:name=".ui.customer.CartActivity" />
        <activity android:name=".ui.customer.ProductDetailsActivity" />
        <activity android:name=".ui.customer.CheckoutActivity" />
        <activity android:name=".ui.customer.OrderDetailsActivity" />
        <activity android:name=".ui.customer.ProfileActivity" />

    </application>

</manifest>
